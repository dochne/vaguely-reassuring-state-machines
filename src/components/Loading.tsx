export function Loading({}) {
  return (
    <div className="loading show text-center" style={{ paddingTop: "40px" }}>
      <div
        className="spinner-border"
        role="status"
        style={{ height: "40px", width: "40px" }}
      ></div>
    </div>
  );
}
